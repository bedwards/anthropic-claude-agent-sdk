name = "cyoa-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[ai]
binding = "AI"

[vars]
GITHUB_REPO = "bedwards/anthropic-claude-agent-sdk"
STORIES_PATH = "apps/choose-your-own-adventure/stories"

# Secrets (set via wrangler secret put):
# - GITHUB_TOKEN: Personal access token with repo write access
